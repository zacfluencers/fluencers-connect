# Influencer Connect

> A two-sided marketplace where brands book influencers for paid content, with money held safely in escrow until the work is delivered.

## Product Definition (fixed)
- **Creators**: create profiles, set a fixed price per job, set availability, show a portfolio, receive booking requests.
- **Brands**: subscribe (£99 / 7-day or £299 / annual), browse creators, request bookings, pay via escrow, receive content.
- **Booking flow**: request → accept → in progress → in review → completed (or refunded). Escrow holds funds until completion. **Max 3 revisions.** Messaging happens per booking.

## Look & Feel (design system)
- **Dark only.** Background `#0B0715`, text `#FBFAFD`, primary accent `#3717B6`, luminous glow `#8469ED`. No other saturated colours.
- **Inter** font, large confident headings (`.h-display` adds tighter tracking).
- Premium texture & motion: a faint grain overlay on every page, a soft animated "aurora" gradient in the hero/auth, glow on primary buttons, and Framer Motion everywhere (page fade+slide, card hover-lift, staggered reveals, scale+fade modals).
- **Reusable UI kit** in `components/ui/`: `Button`/`ButtonLink` (primary/secondary/ghost), `Card`, `Badge`, `Input`/`Textarea`/`Select`/`Field`, `Modal`, `Stepper`, and `motion` helpers (`Reveal`, `RevealOnView`).

## Database (Supabase / Postgres)
Migrations live in `supabase/migrations/`.

**`0001_init.sql`** — the foundation. Three tables:
- **users** — one row per account (id, email, role). `role` is `brand` or `creator`. Linked to Supabase Auth.
- **creator_profiles** — extra info only creators have (name, bio, niche, Instagram, TikTok, availability, price, profile image). One per creator.
- **bookings** — a brand's request to a creator (status, price, revision count, created date). Price is saved at request time so later price changes don't affect existing bookings. Revisions capped at 3.

**`0002_booking_status_declined.sql`** — adds a "declined" status so creators can reject a request without deleting it.

**`0003_auth_user_provisioning.sql`** — when someone signs up, automatically creates their row in `users` with the role they picked. Also lets the two people on a booking see each other's basic account info.

**`0004`–`0008`** — locked down the signup function; added **portfolio** (a `portfolio_items` table + a Supabase Storage bucket for uploaded media — now vertical videos), **favourites** (a `favorites` table), and **follower counts** (Instagram/TikTok numbers on a creator profile).

**`0009_brand_profiles.sql`** — brands get a profile (company name, what they're looking for, budget) and a "Looking for creators" toggle; visible to creators when on.

**`0010_messaging.sql`** — real messaging: `conversations` (between a brand and a creator, optionally tied to a booking) and `messages`. Powers both the booking deal room and direct creator→brand chats.

**`0011_stripe_escrow.sql`** — Stripe Connect escrow: creators get a connected-account id + payout-ready flag; bookings track payment status (`unpaid`/`held`/`released`/`refunded`) and the Stripe payment/transfer/refund ids.

**`0012_creator_attributes_and_rates.sql`** — richer creators + transparent per-service pricing. Replaces the single price with three rates — **UGC**, **Event Day**, **B-Roll** (any a creator doesn't offer is left blank) — and adds **gender**, **age**, and **country** for filtering. Bookings now record which **service** was booked. (The old single price was carried over into the UGC rate, and is kept in sync with the lowest set rate behind the scenes.)

**`0013_brand_media_and_notifications.sql`** + **`0014`** — brands get a **logo, website, Instagram, and TikTok**; a new **`avatars`** storage bucket holds uploaded logos; the **portfolio** bucket's per-file limit was raised to **150MB**; and a **`notifications`** table was added (one row per alert, with read/unread state) so both sides get notified about messages and booking activity. RLS lets people read/clear only their own notifications and create one only for someone they share a booking or conversation with.

**`0017_booking_briefs.sql`** — the **campaign brief** a brand fills in after booking. A **`booking_briefs`** table (1:1 with a booking) holds all the brief fields plus the **product logistics** (mode = none/ship/order, tracking number, product link, discount code). A **`booking_assets`** table holds uploaded reference files, stored in a new public **`briefs`** storage bucket (100MB/file). RLS: **either party** on the booking can read the brief and its files; only the **brand** can create/edit them.

**`0018_booking_deliverables.sql`** — the **creator's delivered content**. A **`booking_deliverables`** table records each uploaded file, stored in a new public **`deliverables`** storage bucket (150MB/file, for video). RLS: **either party** can read the files; only the **creator** on the booking can add/remove them.

## Payments & escrow (Stripe Connect)
- A brand **pays at request time** via Stripe Checkout; the money is **held in escrow** on the platform balance and the booking is created (paid) once Stripe confirms.
- **Approve & complete** → funds are **transferred** to the creator's connected account. **Decline** or a brand **refund/dispute** → funds are **returned** to the brand.
- Creators set up **payouts** (Stripe Express) from their dashboard; they must finish setup before funds can be released.
- A Stripe **webhook** (`/api/stripe/webhook`) is the source of truth for paid bookings and payout-readiness.
- **Setup needed** (env vars in `.env.local` + Vercel): `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SITE_URL` (see `.env.local.example`). Subscriptions are still not built (Stage 2).
- **Demo mode (no Stripe keys):** until those keys are set, "Auto book" / "Request & pay" **skips checkout and creates the booking directly** (marked `unpaid`), dropping the brand straight into the deal room — so the whole booking → brief → accept → complete flow is fully testable without payments. Escrow release/refund also no-op cleanly in this mode. The moment Stripe keys are present, real Checkout takes over and this path never runs.

Notes:
- Every table has Row Level Security on. People only see their own data; creator profiles are public so brands can browse; the two parties on a booking can see each other.
- Not built yet: payments/escrow ledger, per-booking messaging, brand subscriptions, portfolio media.

## Landing Page (`/`)
A high-impact homepage that adapts to who's viewing. **Brands / signed-out visitors** see "Book creators instantly", Browse Creators / Become a Brand CTAs, and a **featured-creator grid**. **Signed-in creators** see a creator-facing hero ("Get booked by brands", Browse Brands / Your dashboard CTAs) and a **grid of brands looking for creators** instead — so the page feels complete from either side. Both grids show a clean **single row** (up to 4) so nothing overflows messily. The hero ends with a **"Trusted by modern brands" endless logo marquee** (seamless loop, soft fades at both edges, pauses on hover).

Below the fold the page tells a full story, in order: **How it works** (a 3-step explainer — Discover, Book at a fixed price, Pay safely on delivery), **Built for both sides** (two big side-by-side cards — "For brands" and "For creators" — each with four check-marked value points and its own CTA, so the page sells to whoever is reading), a **stats band framed with gridlines** (live creator count, Rapid · Content delivery, &lt; 48h · Avg. turnaround, Endless · Content opportunities), and a **final call-to-action panel** (glowing, aurora-lit "One marketplace, zero friction" with Become a Brand / Become a Creator buttons, or a single Browse button when signed in), then a minimal footer. The first stat ("Vetted creators") is the **live count of creator profiles** from the database. The CTAs throughout adapt to who's signed in.

## Accounts & Login
People sign up as either a **brand** (books creators) or a **creator** (gets booked), using email + password (Supabase Auth). A signed-in person sees their email and role in the top navigation, with a Sign out button. **First-time brands** land on a focused **`/welcome`** step to fill in their brand profile (with a "skip for now" link), then land on the **marketplace**; returning brands go straight to the marketplace. Creators land on their dashboard.

### Forgot / reset password
A **"Forgot password?"** link on the sign-in form goes to **`/forgot-password`**, where the user enters their email and Supabase sends a secure reset link. The link lands on **`/auth/callback`** (or **`/auth/confirm`** for token-hash emails), which signs them in with a short-lived recovery session and forwards them to **`/reset-password`** to choose a new password (8+ chars, confirmed). They're then routed in like a normal sign-in. We never reveal whether an email has an account.

**Supabase setup required for the emails to work:** in the Supabase dashboard → **Authentication → URL Configuration**, set the **Site URL** to the live domain and add the app's URLs to **Redirect URLs** (e.g. `http://localhost:3000/**` and `https://<your-domain>/**`). The default reset email works **same-browser**; for reliable **cross-device** resets (request on desktop, open on phone), change the recovery email template to a token-hash link: `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=recovery&next=/reset-password`.

## Pages
- **Browse Creators** (`/marketplace`) — lists every creator with a rich filter bar: **Industry** (multi-select of niches), **Gender**, **Country**, **Available only**, plus a "More filters" panel with sliders for **Age**, **Rate** (budget range), and **minimum Instagram / TikTok followers**. Every filter lives in the web address, so a filtered view can be shared or bookmarked. Each creator card shows their transparent per-service prices, clickable Instagram/TikTok follower counts, an **Auto book** button, and a **Chat** button.
- **Creator Profile** (`/creator/[id]`) — one creator's page: photo, bio, niche, gender/age/country, clickable social handles with follower counts, availability, a 9:16 video portfolio, and a **transparent pricing panel** with a separate **Request & pay** button for each service they offer (UGC / Event Day / B-Roll). Brands can also start a **chat**. Signed-out visitors are prompted to sign in; creators can't book.
- **Sign in / Join** (`/login`, `/signup`) — account creation and login. Signup includes the brand/creator choice.
- **Creator Dashboard** (`/dashboard/creator`) — a **modular two-column layout**. The **left rail** shows a live preview of the creator's **card exactly as brands see it** (plus a link to their public profile) and their **payout setup**. The **right side** is a set of panels: quick stats (new requests / active / completed), **booking requests** (accept/decline), **active bookings**, a **Messages** panel (recent conversations → inbox), the **profile editor** (a **profile photo upload**, niche, gender/age/country, socials + follower counts, and the three service rates — UGC / Event Day / B-Roll), the **portfolio** uploader (9:16 videos), and past bookings.
- **Favourites** (`/favorites`) — role-aware. **Brands** see the **creators** they've saved (via the bookmark on a creator card/profile); **creators** see the **brands** they've saved (via the bookmark on a brand card/profile). Creators never see a save button on creator cards — favouriting creators is a brand-only action.
- **Brand Dashboard** (`/dashboard/brand`) — the same **modular two-column layout** as the creator dashboard. The **left rail** shows a live preview of the brand's **card as creators see it** in the directory (with a "Visible / Hidden" status) plus quick links (browse, saved creators, messages, all bookings). The **right side** has panels for quick stats (active / in escrow / completed), **active bookings** (cards with a mini progress bar), a **Messages** panel, the **brand profile** editor (now with a **logo upload**, **website**, and **Instagram/TikTok**), and history. (Brands now land on the **marketplace** after sign-in/up; the dashboard is one click away in the nav.)
- **Booking Detail / "Deal Room"** (`/bookings/[id]`) — the centrepiece. A full status **stepper**, a participants panel (creator + brand), the agreed price, a **revision counter** (out of 3), a **campaign brief** (see below), a **content delivery** area with **real file uploads** (see below), a clean **message thread**, and the action buttons. The flow now moves **both ways**: alongside the forward action at each stage, the creator gets a **"Back a step"** option (Accepted → back to request, In progress → back to accepted, In review → withdraw submission) so a mis-click or a change of mind is easy to undo. The only one-way actions are the money ones — **Approve** (releases escrow) and **Decline** (refunds) are final. Brands also get dispute/refund via a modal.
- **Content delivery** (inside the deal room) — the **creator uploads the actual deliverables** here (video or images, up to 150MB each) with a live progress bar; they can remove files until the booking completes. The **brand** sees each file with an inline preview and a **download** link. Order of play: the creator uploads files, then clicks **"Submit for review"** to send them over. Files live in a public **`deliverables`** storage bucket; only the creator on the booking can add/remove them, and only the two parties can see them.
- **Campaign brief** (inside the deal room) — booking stays **one-click** (pay → booking created), then the brand fills in a short **brief** here. Kept intentionally lean: **Campaign details** (name, target audience), **Content** (platform, creative brief, call-to-action), **Requirements** (must include, avoid, deadline), **Commercial** (usage rights), a **Product** choice, and **reference file uploads**. The **Product** choice is a toggle: *No product*, *I'll ship it* (reveals a tracking-number field), or *Creator orders it* (reveals a product link + a 100%-off discount code). Only the **brand** can edit; the **creator** sees a tidy read-only version and gets a notification when the first brief is added. A brand with no brief yet lands straight in edit mode (the post-booking nudge). The brief can be edited any time. Files go to a public **`briefs`** storage bucket (100MB each).
- **Invoice / receipt (PDF)** — once a booking is **completed**, both parties get a **Download receipt (PDF)** button in the deal room. It generates a clean A4 PDF on the fly (`/bookings/[id]/invoice`, built with `pdf-lib`) showing the parties, service, amount, total, payment status and a reference number — for tax/records. Demo bookings produce the same receipt marked "no payment taken".

> UI-only for now (no backend yet, per brief): the message thread (composing is local — needs a `messages` table to persist), the content-delivery file area, and the dispute modal. The approve / request-revision / accept / decline actions are fully wired to the real booking flow.
- **My Bookings** (`/bookings`) — every booking you're part of, with its current status. Click one for the full detail.
- **Booking Detail** (`/bookings/[id]`) — shows the status, a progress bar through the flow, both the creator and brand, the agreed price, revisions used (out of 3), and the buttons for your next step.

## How a booking moves
`requested → accepted → in_progress → in_review → completed`. A creator can **decline** a request (→ declined). While a booking is in review, the brand can **request a revision** (sends it back to in progress), up to **3 times**. Each button only appears for the person allowed to press it. **No payments yet** — "completed" just marks the work done; escrow/Stripe comes later.

## Components
- **Nav** — top navigation; auth-aware. **Signed-in:** the full link bar (5 links + account) shows at ≥1024px; below that it collapses to a **hamburger** full-screen menu (`MobileNav`) so it never bleeds off screen (the email only appears at extra-large widths). **Signed-out:** the **Sign in / Get started** CTAs stay visible on every screen size (never hidden behind a menu), with the Browse link showing from ~640px — so the primary actions are always one tap away.
- **MobileNav** — the mobile-only full-screen menu (staggered links, sign-in/out, scroll-lock, Escape-to-close).
- **Avatar** — round profile image (or gradient initial) used across messaging.
- **CreatorCard** — the marketplace card: photo, name, niche, clickable IG/TikTok follower counts, transparent per-service prices, an **Auto book** button, and a **Chat** button.
- **MarketplaceFilters** — the full filter bar (industry multi-select, gender, country, availability + a sliders panel for age, rate, and follower minimums). Writes everything to the URL.
- **RangeSlider** (`DualRange` / `MinSlider`) — the two-handle range and single "minimum" sliders used in the filters.
- **AutoBookButton** — "Auto book": a fast, pre-filled booking request; the brand picks a service and pays into escrow in one step (the creator still approves).
- **MessageCreatorButton** — starts (or reopens) a brand→creator direct chat.
- **ServiceBooking** — the transparent pricing panel on a creator profile, one **Request & pay** button per service.
- **SocialIcons** — the Instagram and TikTok glyphs.
- **DashboardPanel** (`Panel` / `Stat`) — the shared modular card + stat tile used by both the creator and brand dashboards, so they stay visually in sync.
- **DealRoomLink** — the single, clearly-labelled **"Open deal room"** button used on every booking surface (lists, cards, dashboards) so the way into a booking is obvious and consistent.
- **NotificationBell** — the bell + unread badge + dropdown in the top nav; lists recent alerts, marks them read, and links through to the relevant page.
- **ImageUpload** — a reusable single-image uploader (used for brand logos) that stores to the `avatars` bucket and submits the URL with its form.
- **BrandCard** — now shows the brand's uploaded logo (or initial), the "Looking for creators" badge on its own row, and clickable website / Instagram / TikTok links.

## Notifications
Both brands and creators get notifications (a **bell** in the top nav with an unread count). They're created for: a **new message**, a **new booking request** (creator), and every **booking update** — accepted, declined, started, submitted for review, revision requested, approved/completed, or cancelled/refunded. Opening one marks it read and jumps to the booking or conversation. (Counts refresh when you move around the site — there's no live push yet.)
- **AuthForm** — the sign in / sign up form.
- **CreatorProfileForm** — where a creator fills in their bookable profile.
- **RequestBookingButton** — the "Request Booking" action on a creator's page.
- **BookingActions** — the accept/decline/advance buttons on a booking, shown only to whoever can act.
- **StatusBadge** — the little coloured status label used across booking pages.

## Connecting the database
The site reads creators from Supabase. To switch it on, copy `.env.local.example` to `.env.local` and paste in your Supabase URL and anon key (from the Supabase dashboard → Project Settings → API). Until then, the marketplace shows a friendly "connect your database" message instead of erroring.

## Recent Changes
- 2026-07-10: **Went live.** Custom domain `connect.fluencersgroup.com`, Stripe switched from test to live mode (real payments + creator payouts, 10% platform fee), live webhook wired, all production env vars verified. Also enabled **promo codes** on the subscription checkout so launch discounts / coupons work.
- 2026-06-17: Created the initial database schema (users, creator_profiles, bookings).
- 2026-06-17: Built the creator marketplace — `/marketplace` listing with niche + availability filters, and `/creator/[id]` profile pages. Reads live from Supabase. No booking or payment logic yet.
- 2026-06-17: Added email/password accounts (Supabase Auth) with brand/creator roles, and the full booking flow — request, accept/decline, the requested→accepted→in_progress→in_review→completed status journey, revision requests (max 3), a creator dashboard, and booking pages. Still no payments or subscriptions.
- 2026-06-17: Added **portfolio image uploads** (stored in Supabase Storage), a **niche dropdown** of popular creator niches (replacing free text), **favourites** (save creators with a heart), and **follower counts** on profiles/cards.
- 2026-06-17: **Full visual redesign** to a dark-first, premium SaaS look on the strict brand palette — new landing page, reusable `components/ui/` design system, Framer Motion throughout (grain + glow + animated gradient), a brand dashboard, and a "deal room" booking detail page (stepper, message thread, content delivery, revision counter, dispute modal).
- 2026-06-17: Restyled toward a clean dark-mode-Airbnb feel (borderless photo cards, calmer buttons, more whitespace, responsive type scale, save icon).
- 2026-06-17: **Portfolios are now 9:16 vertical videos**; **creators can no longer see/favourite other creators** (they browse the new **Brands** directory instead); added **brand profiles** with a "Looking for creators" toggle; and shipped **real persisted messaging** (powers both the brands outreach and the booking deal room).
- 2026-06-18: **Transparent per-service pricing + richer creator profiles + powerful search.** Creators now set three rates (**UGC / Event Day / B-Roll**) and capture **gender, age, country**. Creator cards show every rate, clickable **Instagram/TikTok follower counts**, an **Auto book** button (fast pre-filled booking request), and a **Chat** button. The marketplace gained a full filter bar — **industry (multi-select), gender, country, availability**, plus sliders for **age, rate, and minimum IG/TikTok followers**. Bookings record which service was booked.
- 2026-06-18: **Modular dashboards** for both creators and brands (card-as-clients-see-it on the left, modular panels on the right), and the **Become a creator/brand** links now pre-select the right role on signup.
- 2026-06-18: **Notifications for both sides** (a bell in the nav, with messages + every booking update), **brand logo upload + website/socials** (logo falls back to the company initial; "Looking for creators" badge moved to its own row), larger **150MB** portfolio video uploads, and cleaner Instagram/TikTok icons.
- 2026-06-18: Portfolio videos now show their **first frame as a thumbnail** (no more black boxes), and every booking has a clear **"Open deal room"** button across lists, cards, and dashboards.
- 2026-06-18: **Messages upgrades** — conversations are labelled **deal room (+ live status)** vs **direct message**; inside a conversation you can **view the other party's profile**, **open the deal room**, or **Book now** (brand → creator, when there's no active booking). Added a **brand profile page** (`/brand/[id]`).
- 2026-06-18: Creator profile image is now an **upload** (not a URL). The favourite button no longer shows to creators on creator cards/profiles, and **creators can now favourite brands** (bookmark on brand cards/profiles); the **Favourites** page is role-aware (brands→creators, creators→brands).
- 2026-06-18: Portfolio video uploads now show a **live progress bar** (with a per-file counter) instead of a silent "Uploading…", via a direct browser→storage upload that reports progress.
- 2026-06-18: **Fixed** creator-profile fields (niche, etc.) sometimes needing to be saved twice — the form fields are now **controlled**, so React 19's automatic post-save form reset can't blank them.
- 2026-06-18: **Typography + spacing tidy-up** — refined the fluid type scale (more balanced heading line-heights and a cleaner lead size) and unified every content page onto one responsive vertical rhythm (`py-14 sm:py-20`) so whitespace feels consistent across all pages and screen sizes.
- 2026-06-18: Fixed the **dashboards cropping on mobile** — the two-column grid now uses shrinkable tracks (`minmax(0,1fr)` + `min-w-0`) so wide content (long emails, escrow totals) wraps/truncates instead of forcing horizontal overflow; stat tiles are more compact on small screens.
- 2026-06-18: **Mobile pass** — a smooth full-screen **hamburger menu**, the hero eyebrow now scales to stay on one line with a **gently pulsing dot**, and the hero headline/spacing scale down cleanly on small phones. The mobile menu and **modals render through a portal** to the page body so they always cover the real viewport (the page-transition wrapper's CSS transform was trapping `position: fixed` overlays — this also fixed the deal-room dispute modal). Notification dropdown width is now responsive.
- 2026-06-18: Social-handle fields now show a fixed **`@`** prefix and website fields a fixed **`https://`** prefix (you type just the handle/domain), so links and handles are always saved in the right format.
- 2026-06-18: **Signed-out visitors see teaser cards** — name, photo, niche, and follower counts, but **pricing and booking are hidden** behind a "Sign up to see pricing & book" button (on browse, the homepage, and creator profiles). Signed-in brands still get full pricing, auto-book, chat, and favourites.
- 2026-06-18: First-time **brands** get a focused **`/welcome`** profile-setup step (then land on the marketplace). Homepage "Vetted creators" stat shows the live creator count with a trailing **+**; second/fourth stats reworded.
- 2026-06-18: **Auto-fill follower counts** — a button on the creator form scrapes Instagram/TikTok follower counts from the entered @handles (unofficial, best-effort, demo-only; falls back to manual entry).
- 2026-06-19: **Fleshed-out landing page** — added new storytelling sections below the hero: a 3-step **How it works** explainer, a **"Built for both sides"** block (side-by-side For brands / For creators value cards), and a glowing **final call-to-action** panel. Reordered so the page builds from hook → proof → how → why → numbers → convert. All CTAs adapt to who's signed in. (Testimonials were considered but removed for now until real quotes are available.)
- 2026-06-19: **Campaign briefs in the deal room** — after a brand books (still one click), they fill in a structured **brief** on the booking page: campaign details, content (platform, deliverables, creative brief, talking points, CTA), requirements (must-include, avoid, deadline), commercial (payment notes, usage rights), **reference file uploads**, and a **product toggle** — either the brand ships it (tracking number) or the creator orders it (product link + 100%-off code). The creator sees a read-only version and is notified when the brief is added. New `booking_briefs` + `booking_assets` tables and a `briefs` storage bucket (migration 0017).
- 2026-06-19: **Demo booking mode** — with no Stripe keys configured, booking no longer shows "payments aren't set up"; it creates the booking directly and opens the deal room, so the full flow (including the new brief) is testable without Stripe. Escrow release/refund fall back to the user's session when the service-role key is also absent.
- 2026-06-19: **Condensed the brief** (dropped Objective, Deliverables, Talking points, Payment terms — the creative brief field now covers deliverables) and added an **auto-generated PDF receipt** for completed bookings (download button in the deal room; generated with `pdf-lib`).
- 2026-06-19: **Real content delivery + two-way flow.** The deal room's "Content delivery" panel was a mockup — it now has **working uploads**: the creator uploads deliverables (video/images, 150MB each, with progress), the brand previews and downloads them (shown in a 9:16 frame so nothing is cropped). Added **backward steps** so the booking can move both ways between stages (creator can step back / withdraw a submission); Approve and Decline stay final because they move money. New `booking_deliverables` table + `deliverables` storage bucket (migration 0018).
- 2026-06-19: **Forgot / reset password.** A "Forgot password?" link on sign-in → `/forgot-password` emails a Supabase reset link → `/auth/callback` (or `/auth/confirm`) → `/reset-password` to set a new one. Needs the Site URL + Redirect URLs configured in Supabase (see Accounts & Login).
- 2026-07-07: **Security hardening #1 — locked-down bookings (migration 0019).** Added a database trigger (`booking_guard`) so the money fields on a booking (price, payment status, all Stripe IDs, and who the two parties are) can only be written by trusted server code — never by a signed-in user poking the API directly. When a brand creates a booking the price is forced to the creator's real rate (can't be set to £0) and it always starts unpaid; the two parties can still move it through its status steps. Verified with a simulated attack — a forged £0 / pre-paid booking was corrected automatically.
- 2026-07-07: **Security hardening #2 — private files for deliverables & briefs (migration 0020).** The two storage buckets that hold a creator's delivered content and a brand's brief attachments used to be public — anyone with the file link could open them without signing in. They're now **private**: files are served through **signed links that expire after 1 hour**, and only the two people on that booking can generate one. No change to how you upload or view files in the deal room.
- 2026-07-07: **Security hardening #3 — graceful error handling.** If something breaks (a database or payment-provider hiccup), the site now shows a **calm, branded "Something went wrong" card with a Try again button** instead of a raw crash page — plus a matching **404 page** for broken/unknown links. The **checkout and payout-setup buttons** now show a friendly "couldn't reach the payment provider, try again" message instead of erroring, and the **payment webhook** now retries safely if a paid booking can't be saved on the first try (so a paid booking is never silently lost). New files: `app/error.tsx`, `app/global-error.tsx`, `app/not-found.tsx`.
- 2026-07-07: **Automated tests added (Playwright).** First end-to-end tests covering the critical journeys — public pages load, the branded 404 shows, sign-up reaches the next step, a wrong password shows an error (not a crash), and a brand can book a creator. Run them with `npm run test:e2e`. See the **Testing** section below.
- 2026-07-07: **Stripe env docs + webhook events.** Added `.env.example` (placeholders only) and a README env-var guide for the Vercel setup (which keys are browser-safe `NEXT_PUBLIC_*` vs server-only secrets). Expanded the payment webhook (`/api/stripe/webhook`) to also handle payment failures, refunds (including refunds made from the Stripe dashboard — a still-held booking is marked refunded), and subscription events (acknowledged only; billing isn't built). Every event is logged by type/id (never secrets), and transient failures return an error so Stripe retries. Signature checking (raw body + `STRIPE_WEBHOOK_SECRET`) is unchanged. **The webhook's database writes need `SUPABASE_SERVICE_ROLE_KEY` set in Vercel.**
- 2026-07-07: **Live payment flow verified (Stripe test mode).** Booking a creator now goes through real Stripe Checkout end-to-end — payment → webhook → escrow held in the database — confirmed working with a test-mode transaction. Also surfaced the real Stripe error in server logs when checkout/payout-onboarding fails (previously hidden behind a generic message), which is what pinned down a live-vs-test key mix-up.
- 2026-07-07: **Deal room opens at the top.** Fixed the deal-room page loading scrolled partway down — the animated page-transition wrapper was interfering with the browser's scroll-to-top, so the page now resets to the top on every navigation (`app/template.tsx`).
- 2026-07-07: **Creator payouts (Stripe Connect) working + friendlier.** Creators can now connect Stripe to get paid. They onboard as **individuals** (never a company), and the "business" fields are **pre-filled** from their profile so there's as little to enter as possible — just identity + bank + a one-time ID check (legally required to pay anyone). GB→GB payouts use Stripe's standard agreement. The dashboard's "Payouts active/incomplete" status **syncs directly from Stripe** on load, so it updates reliably even if the background webhook is delayed.
- 2026-07-07: **Brand subscriptions (Stripe Billing).** Brands can subscribe on their dashboard's **Membership** panel — **Weekly** or **Annual** — via Stripe Checkout in subscription mode. Prices are resolved from Stripe **lookup keys** (`STRIPE_PRICE_BRAND_WEEKLY_LOOKUP_KEY`, `STRIPE_PRICE_BRAND_ANNUAL_LOOKUP_KEY`), so no price IDs are hardcoded. Subscribed brands get a **Manage plan** button (Stripe billing portal) to update/cancel. Status is kept in a new `brand_billing` table (migration 0021) that brands can only **read** — all writes come from the Stripe webhook (subscription events) + an on-return sync, so a brand can't mark themselves "subscribed" without paying. The webhook now separates subscription checkouts from booking payments.
- 2026-07-09: **Pre-launch security hardening (3 fixes).** (1) **Booking status guard** (migration 0023): the money-guard trigger now also blocks illegal status jumps — a booking party can only move through the legal workflow steps; `completed`/`declined`/`refunded` are reachable only via the trusted service-role escrow flow. Verified with a simulated attack (jump to `completed` blocked, legal step allowed). (2) **Production security headers** (`next.config.ts`): `X-Content-Type-Options`, `X-Frame-Options: SAMEORIGIN` (keeps the Sanity preview working), `Referrer-Policy`, HSTS, `Permissions-Policy`. (3) **Stripe success page** now verifies the signed-in user is the brand who paid before resolving the booking. *(Reviewed an external AI security list: money fields were already locked, uploads already validate type/size, and a claimed duplicate-variable bug didn't exist.)*
- 2026-07-09: **Visual (click-to-edit) editing + editable logo slider.** Added Sanity **Presentation** — a live preview of the homepage inside the Studio where you click any text to jump to its field and see edits update as you type (needs a read-only `SANITY_API_READ_TOKEN`). Also made the hero **"Trusted by" logo slider** editable: a new **Trusted by logos** field on the Homepage where you upload brand logo images (falls back to the placeholder names when empty). New files: `lib/sanity/live.ts`, `lib/sanity/image.ts`, draft-mode enable/disable routes; the Studio config gained the Presentation tool.
- 2026-07-09: **Editable landing page via Sanity CMS.** Added an embedded content editor at **`/studio`** (Sanity project `nqj4ihoz`). The public homepage's marketing copy — hero (badge, headline, sub-headline, buttons), "How it works" (heading + 3 steps), "Built for both sides" (headings + both cards' bullet points), and the final call-to-action — now reads from Sanity, **falling back to the built-in wording** whenever a field is blank, so the page can never look empty or break (even if Sanity is unreachable or unconfigured). The site nav is hidden on `/studio` so the editor is full-screen. Needs three public env vars (`NEXT_PUBLIC_SANITY_PROJECT_ID/DATASET/API_VERSION`) in Vercel, plus CORS origins added in the Sanity dashboard. See **Editing content (Sanity CMS)** below.
- 2026-07-09: **Scheduled social refresh is live.** A weekly Vercel Cron (Mondays 06:00 UTC, `vercel.json` → `/api/cron/refresh-social?cohort=active`) now auto-refreshes active creators' follower counts, engagement and avatars, guarded by `CRON_SECRET`. Verified end-to-end in production — a manual "Run" re-synced the live creator and the follower count updated from real API data.
- 2026-07-09: **Creator social enrichment (ScrapeCreators).** Replaced the old best-effort follower scraper with a proper server-side enrichment system. On the creator profile, the button is now **“Auto-fill from social profiles”** (states: “Fetching profile data…” / “Profile data updated” / a friendly error) — it calls a secure server route (`/api/creator/enrich-social-profile`) that reads the **`SCRAPECREATORS_API_KEY`** (server-only, never exposed to the browser) and pulls followers, avatar, bio, display name and — where the data allows — engagement. Imported data is saved to a new **`creator_social_accounts`** table (one row per creator+platform, no duplicates, full raw response kept) and key display fields are mirrored onto the creator card. **Creator cards** now show each platform’s followers plus a subtle **average-engagement** line, and fall back to the imported **Instagram → TikTok avatar** when a creator hasn’t uploaded their own photo (a manually uploaded photo is never overwritten). Reusable **refresh functions** (`refreshCreatorSocialData`, active/featured/inactive cohorts) and a guarded cron route (`/api/cron/refresh-social`, needs `CRON_SECRET`) are in place for scheduled syncs — see **Creator social enrichment** below for how to switch cron on. New migration `0022`.
- 2026-07-09: **Production email is live (Resend).** Connected **Resend** as the email provider in Supabase's SMTP settings, sending from **`no-reply@fluencersgroup.com`** on a DNS-verified domain (lands in the inbox, not spam). Raised Supabase's email rate limit to 100/hour. This fixes the built-in mailer's tight rate limit — sign-up confirmations and password resets now send reliably. **Go-live checklist item #1 complete.**
- 2026-07-09: **Go-live checklist added to the README.** A plain-English, tick-box **"Going live (production checklist)"** section covering, in order: connect a real **email/SMTP** provider (built-in mailer is rate-limited), switch **Stripe** from test to live (activate account + Connect, live keys, recreate subscription prices, live webhook, platform fee), **Supabase** auth settings (Site/Redirect URLs, leaked-password protection), **domain + `NEXT_PUBLIC_SITE_URL`**, an env-var sanity check, and a final live smoke test (automated flows + one real small payment through the whole money path incl. refund). Each item says what/why/where.
- 2026-07-09: **Security follow-up — honest delete/permission reporting.** Reviewed the file-delete and notification actions against the live database rules and confirmed the database already blocks cross-account access (only a booking's **creator** can delete their delivered files, only the **brand** can delete brief attachments, only the owner can mark their notifications read — no data leak). Fixed a hidden bug this surfaced: the delete actions used to report **"success" even when nothing was actually deleted** (e.g. if the wrong account tried). They now double-check ownership, only report success when a file was genuinely removed, and log (rather than crash on) any leftover storage cleanup. Also confirmed the security advisor is clean apart from the optional "leaked password protection" toggle. Files: `app/actions/deliverables.ts`, `app/actions/brief.ts`, `app/actions/notifications.ts`.
- 2026-07-09: **Quick links trimmed for unsubscribed brands.** On the brand dashboard, the "Quick links" panel now only shows **Browse creators** for a brand without a subscription (Saved creators / Messages / All bookings are hidden until they subscribe) — so no link points at a locked feature. Subscribed brands still see all four.
- 2026-07-09: **Automated tests for the subscription gating + fixes.** Added a full **gating** test suite (`e2e/gating.spec.ts`) that proves an unsubscribed brand is browse-only (single "Subscribe to see more" CTA, no book/message buttons, trimmed nav, disabled "Looking for creators" toggle) **and** that a subscribed brand keeps full access. **All gating + booking tests were run and pass against the live production site.** Also fixed two pre-existing test issues: the sign-up test used `@example.com` (which Supabase rejects) and asserted a too-strict outcome — it now uses a valid domain and checks the flow responds cleanly (advance **or** a friendly message), and the booking test now finds the profile-page **"Request & pay"** button. See the **Testing** section. Surfaced a production note: Supabase's built-in signup email is rate-limited — connect a real email provider (SMTP) before launch.
- 2026-07-07: **Free brands are gated (must subscribe to interact).** Brands without an active subscription can **browse and admire** creators only. Everything interactive is removed for them: **booking, messaging, favouriting, and pricing** are hidden, and each creator card shows a single full-width **"Subscribe to see more"** button (creator profiles show the same as a teaser). They also **can't tick "Looking for creators"** (the toggle is greyed out — subscribers only), and the **nav** drops Favourites/Messages/Bookings and shows a **Subscribe** button. Everything is enforced **server-side** too (booking, messaging, favouriting, and the directory-listing toggle all re-check the subscription, so nothing can be bypassed via the API). Creators are never gated. If Stripe billing isn't configured (local/demo), the gate is off so the app still works.

## Testing
Automated browser tests live in the `e2e/` folder and use **Playwright**. Full
run instructions + how to seed the test accounts are in **`e2e/README.md`**.
- **Run them:** `npm run test:e2e` (or `npm run test:e2e:ui` for the visual runner). By default they test against your running preview at `http://localhost:3000`.
- **Always-on (no login):** homepage / marketplace / login / signup render, the 404 page, the sign-up flow responds cleanly, and a failed login shows a friendly error.
- **Subscription gating** (`e2e/gating.spec.ts`): proves an unsubscribed brand is browse-only (one "Subscribe to see more" CTA, no book/message/favourite, trimmed nav, disabled directory toggle) **and** a subscribed brand keeps full access. **Proven passing against production.**
- **Booking** (`e2e/booking.spec.ts`): a subscribed brand books a creator and reaches Stripe Checkout. **Proven passing against production.**
- **The signed-in flows need two seeded brand accounts** (one subscribed, one not) and must run against an environment where Stripe billing is configured — locally the app is in demo mode so the gate is off. Point them at production with the env vars in `e2e/README.md`; without those creds the signed-in tests **skip**, so the default run is always green.
- **Good next tests to add** (in priority order): send a message, cancel/refund a booking, and the creator's payout onboarding.

## Creator social enrichment
When a creator adds their Instagram/TikTok handle and clicks **“Auto-fill from social profiles”**, the app pulls their public profile data via the **ScrapeCreators** API. This is all server-side and safe:
- The API key lives only in **`SCRAPECREATORS_API_KEY`** (server-only — never sent to the browser, never `NEXT_PUBLIC_`). Get it from ScrapeCreators and set it in Vercel.
- Imported data is saved to **`creator_social_accounts`** (followers, following, posts, avatar, bio, display name, engagement, plus the full raw response and a `last_synced_at` stamp). Card-facing bits (followers, avatar, engagement) are copied onto `creator_profiles` so the marketplace shows them with no extra queries.
- A creator can only enrich **their own** profile; the profile page never breaks if a fetch fails (errors are logged, the button shows a friendly message).

**Scheduled refresh.** A weekly cron for active creators is configured in `vercel.json` (`/api/cron/refresh-social?cohort=active`, Mondays 06:00 UTC). It only needs one thing to run: a **`CRON_SECRET`** env var in Vercel (any long random string). Vercel automatically sends that secret as a Bearer token so only Vercel can trigger the route; without the secret set, the route stays disabled (503). The cohort helpers (`refreshActiveCreatorsSocialData`, inactive, featured) live in `lib/social/enrichment.ts` — add more cron entries in `vercel.json` for other cadences. *(“Featured” = daily needs a `featured` flag on creators first — it’s a documented stub until then. Vercel’s free Hobby plan allows up to 2 cron jobs at daily-or-less frequency; weekly is fine.)*

> **Note on field mapping:** the exact JSON the provider returns can vary, so the normaliser probes several likely paths and we always store the raw response. If a field (e.g. engagement) comes back empty for a platform, it just shows nothing — and we can fine-tune the mapping against a real saved response without re-fetching.

## Editing content (Sanity CMS)
You can edit the homepage marketing copy yourself — no code:
1. Go to **`/studio`** on your site (e.g. `…/studio`) and sign in with Sanity.
2. Click **Homepage**, edit any field, and hit **Publish**.
3. The site updates within a minute.

**What's editable:** the hero (badge, headline, sub-headline, button labels, **"Trusted by" logos** — upload brand images), "How it works" (heading + the three steps), "Built for both sides" (headings + both cards' bullet points), and the final call-to-action. Anything left blank falls back to the built-in wording, so you can't break the page. *(These control the default, brand-facing view; the signed-in-creator variant stays in code.)*

**Visual editing (click-to-edit):** in the Studio, open **Presentation** (a live preview of your site). Click any text on the preview to jump straight to its field; edits show as you type before publishing. This needs a read-only **`SANITY_API_READ_TOKEN`** (Sanity → API → Tokens → "Viewer") set in Vercel (+ `.env.local` for local). Without it, editing still works via the normal fields.

**Setup notes (one-time, in Vercel + Sanity):**
- Env vars in Vercel: `NEXT_PUBLIC_SANITY_PROJECT_ID`, `NEXT_PUBLIC_SANITY_DATASET` (`production`), `NEXT_PUBLIC_SANITY_API_VERSION` (`2024-10-01`).
- In **manage.sanity.io → API → CORS Origins**, add your site origins (e.g. `http://localhost:3000` and your live URL) with credentials allowed, so the Studio can connect.
- The dataset must be **public** for the site to read published content without a token.

## Two-sided access (who sees what)
- **Brands** browse the **creator** marketplace, favourite creators, and book them.
- **Creators** don't see or favourite other creators — instead they browse the **Brands** directory (`/brands`) of brands that are "looking for creators" and can **message** them directly.
- **Portfolios are vertical videos** (9:16). Creators upload MP4/MOV on their dashboard; they play in a locked 9:16 frame on their profile.

## Messaging (`/messages`, `/messages/[id]`)
Real, saved conversations. Started two ways: a creator messaging a brand from the Brands directory, or the per-booking thread inside the deal room. Each conversation is private to its two people.

- **Avatars:** the inbox, the conversation header, and the dashboard message panels show the other person's **profile photo** (creator photo or brand logo, with a gradient-initial fallback) so you can see at a glance who's messaging.
- **Inbox labels:** every conversation shows whether it's a **deal room** (with the booking's live **status** badge) or a **direct message**.
- **Inside a conversation:** the header has the counterpart's name linking to their profile, a **View profile** button, and either **Open deal room** (for booking conversations) or, for a brand messaging a creator with no active booking, a **Book now** button.
- **Brand profile page** (`/brand/[id]`) — a brand's public page (logo, about, budget, website/socials). Creators can reach it from messages, the Brands directory, or the deal room; a relationship-based RLS rule lets a creator view a brand they share a conversation or booking with even if the brand has paused "looking for creators".

## Follower counts — auto-fill (best-effort) + manual
Follower counts are entered **manually**, with an optional **"Auto-fill from handles"** button on the creator profile form that tries to read the public follower count for the entered Instagram/TikTok @handles and fill the numbers in (still editable). This uses an **unofficial best-effort scraper** (`lib/social/scrape.ts` + the `fetchFollowers` action): Instagram via its public web-profile JSON endpoint, TikTok via the follower count embedded in the profile page. It's intended for **demos** — it can be rate-limited, blocked, or broken by site changes (especially from cloud/datacenter IPs like Vercel's), in which case it returns nothing and the creator enters the number manually. **Instagram in particular rate-limits an IP after repeated hits** (returns 401), so the save only re-fetches a platform when the handle is new/changed or no count exists yet — it won't re-hit on every save. TikTok is far more lenient. ⚠️ Scraping is against the platforms' terms of service; the supported production path is a paid creator-data provider (Modash/Phyllo) or the official APIs — the seam for that is `lib/social/sync.ts`, and the DB columns (instagram_followers, tiktok_followers, followers_synced_at) are ready.

## How to Customize
- **To add to the database**: create a new numbered file in `supabase/migrations/` (e.g. `0002_messaging.sql`). Never edit an already-applied migration.
